<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.moaz</groupId>
  <artifactId>taskmanager</artifactId>
  <version>1.0-SNAPSHOT</version>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <release>25</release>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.1</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <shadedArtifactAttached>false</shadedArtifactAttached>
              <finalName>${project.artifactId}-${project.version}</finalName>
              <transformers>
                <transformer>
                  <mainClass>${mainClass}</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.tentackle</groupId>
        <artifactId>tentackle-jlink-maven-plugin</artifactId>
        <version>21.16.3.0</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>jlink-and-jpackage</id>
            <goals>
              <goal>jlink</goal>
              <goal>jpackage</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <mainModule>${moduleName}</mainModule>
          <mainClass>${mainClass}</mainClass>
          <launcher>TaskManagerApp</launcher>
          <jlinkImageName>taskmanager-runtime</jlinkImageName>
          <jpackageExecutable>jpackage</jpackageExecutable>
          <jpackageType>exe</jpackageType>
          <jpackageOutputDirectory>${project.build.directory}/installer</jpackageOutputDirectory>
          <includeJars>
            <includeJar>${project.build.directory}/${project.artifactId}-${project.version}.jar</includeJar>
          </includeJars>
          <verbose>true</verbose>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <repositories>
    <repository>
      <id>central</id>
      <url>https://repo.maven.apache.org/maven2</url>
    </repository>
  </repositories>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <moduleName>com.moaz.taskmanager</moduleName>
    <javafx.version>23.0.1</javafx.version>
    <mainClass>com.moaz.taskmanager.MainApp</mainClass>
  </properties>
</project>
